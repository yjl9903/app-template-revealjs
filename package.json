{
  "name": "app-template-revealjs",
  "version": "0.0.1",
  "author": "XLor",
  "license": "MIT",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "dependencies": {
    "reveal.js": "^4.1.0"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "snowpack": "^3.0.1"
  }
}
